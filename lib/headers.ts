import { localeUtils } from '@/modules/locale';

export const getCurrentLocale = localeUtils.getCurrentLocale.bind(localeUtils);
export const getCurrentUrl = localeUtils.getCurrentUrl.bind(localeUtils);
export const getCurrentLocaleAndUrl = localeUtils.getCurrentLocaleAndUrl.bind(localeUtils);
